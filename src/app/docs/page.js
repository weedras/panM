import Link from "next/link";

export default function Docs() {
  return (
    <main style={{ minHeight: "100vh", padding: "64px" }}>
      <div className="hero-gradient" style={{ top: "10%" }}></div>
      <div style={{ maxWidth: "800px", margin: "0 auto" }}>
        
        <Link href="/" style={{ color: "var(--muted)", textDecoration: "none", marginBottom: "32px", display: "inline-block", fontSize: "14px" }}>
          &larr; Back home
        </Link>
        
        <h1 style={{ fontSize: "56px", marginBottom: "24px", letterSpacing: "-2px", lineHeight: 1.1 }}>
          Platform <span className="gradient-text">Documentation</span>
        </h1>
        
        <p style={{ color: "var(--muted)", fontSize: "20px", marginBottom: "64px", lineHeight: 1.6 }}>
          Learn how to upload your GenBank files, configure metadata, and interpret the phylogenetic trees generated by our pipeline.
        </p>

        <div className="glass-panel" style={{ padding: "40px", marginBottom: "32px" }}>
          <h2 style={{ fontSize: "24px", marginBottom: "16px", borderBottom: "1px solid rgba(255,255,255,0.1)", paddingBottom: "16px" }}>1. Input Data Requirements</h2>
          <p style={{ color: "var(--muted)", lineHeight: 1.6, marginBottom: "16px" }}>
            panM accepts standard GenBank (`.gbk`) files containing both nucleotide sequences and functional annotations. You must provide at least 2 strains for comparative analysis, though the platform is optimized for datasets of 10 to 500 strains.
          </p>
          <ul style={{ color: "var(--muted)", lineHeight: 1.6, marginLeft: "24px" }}>
            <li>Files must end in the `.gbk` extension.</li>
            <li>Ensure genes are properly annotated with CDS features.</li>
            <li>Unique strain identifiers should be used for the file names (e.g., `Strain_A.gbk`).</li>
          </ul>
        </div>

        <div className="glass-panel" style={{ padding: "40px", marginBottom: "32px" }}>
          <h2 style={{ fontSize: "24px", marginBottom: "16px", borderBottom: "1px solid rgba(255,255,255,0.1)", paddingBottom: "16px" }}>2. Analysis Pipeline</h2>
          <p style={{ color: "var(--muted)", lineHeight: 1.6, marginBottom: "16px" }}>
            Once your files are uploaded to our secure queue, our cloud workers initiate the pipeline:
          </p>
          <ol style={{ color: "var(--muted)", lineHeight: 1.6, marginLeft: "24px" }}>
            <li><strong>Translation & Alignment:</strong> All CDS regions are extracted and translated. DIAMOND performs an all-vs-all homology search.</li>
            <li><strong>Clustering:</strong> The MCL algorithm clusters the homologies into distinct orthologous gene groups.</li>
            <li><strong>Tree Building:</strong> FastTree generates a robust phylogenetic tree from the core genome SNPs.</li>
          </ol>
        </div>

        <div className="glass-panel" style={{ padding: "40px" }}>
          <h2 style={{ fontSize: "24px", marginBottom: "16px", borderBottom: "1px solid rgba(255,255,255,0.1)", paddingBottom: "16px" }}>3. Interpreting Results</h2>
          <p style={{ color: "var(--muted)", lineHeight: 1.6, marginBottom: "16px" }}>
            The resulting dashboard provides several interactive panels:
          </p>
          <ul style={{ color: "var(--muted)", lineHeight: 1.6, marginLeft: "24px" }}>
            <li><strong>Strain Tree:</strong> The main phylogenetic tree showing evolutionary divergence.</li>
            <li><strong>Core vs Accessory:</strong> A chart detailing the size of the core genome versus the accessory genome.</li>
            <li><strong>Gene Search:</strong> A datatable allowing you to search for specific antimicrobial resistance (AMR) or virulence genes across your cohort.</li>
          </ul>
        </div>
        
      </div>
    </main>
  );
}
